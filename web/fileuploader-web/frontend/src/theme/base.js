import variables from "../css/common/variables/export.module.scss";

const theme = {
    global: {
        icons: {
            small: variables.iconSizeSmall,
            medium: variables.iconSizeMedium,
            large: variables.iconSizeLarge,
            xlarge: variables.iconSizeXlarge,
        }
    }
}

export default theme